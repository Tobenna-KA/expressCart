{{> themes/MarieLyne/front-menu}}
<div class="container">
  <div class="row">

    <div class="col-lg-3">

      <h1 class="my-4">{{@root.config.cartTitle}}</h1>
      <div class="list-group">
        <a href="#" class="list-group-item">Category 1</a>
        <a href="#" class="list-group-item">Category 2</a>
        <a href="#" class="list-group-item">Category 3</a>
      </div>

    </div>

    <!-- SHOW IF NO PRODUCTS -->
    {{#ifCond results.length '==' 0}}
    <div class="col-lg-9">
      <p class="text-danger">{{ @root.__ "No products found" }}</p>
    </div>
    {{/ifCond}}
    <div class="col-lg-9">

      <div class="row">
        {{!-- LOOP THROUGH PRODUCTS --}}
        {{#each results}}
        <div class="col-lg-4 col-md-6 mb-4 store-product">
          <div class="product-wrapper">
            <div class="container">
              <div class="top">
                {{#if productPermalink}}
                <a href="/product/{{this.productPermalink}}">
                  {{#if productImage}}
                  <img src="{{this.productImage}}" alt="">
                  {{else}}
                  <img src="/uploads/placeholder.png" alt="">
                  {{/if}}
                </a>
                {{else}}
                <a href="/product/{{this._id}}">
                  {{#if productImage}}
                  <img src="{{this.productImage}}" alt="">
                  {{else}}
                  <img src="/uploads/placeholder.png" alt="">
                  {{/if}}
                </a>
                {{/if}}
              </div>
              {{#if variants}}
              <div class="bottom">
                <div class="left">
                  <div class="details">
                    {{#if productPermalink}}
                    <a href="/products/{{this.productPermalink}}">
                      {{this.productTitle}}
                    </a>
                    {{else}}
                    <a href="/products/{{this._id}}">
                      {{this.productTitle}}
                    </a>
                    {{/if}}
                    {{#if @root.config.showHomepageVariants}}
                    <select id="productVariant-{{this._id}}" class="productVariant form-control mb-2">
                      {{#each variants}}
                      <option value="{{this._id}}" data-price="{{this.price}}">
                        {{this.title}} - {{currencySymbol @root.config.currencySymbol}}{{this.price}}
                      </option>
                      {{/each}}
                    </select>
                    {{else}}
                    <h4 class="product-price mp-0 text-center">
                      {{currencySymbol @root.config.currencySymbol}}{{formatAmount variants.0.price}}
                    </h4>
                    {{/if}}
                    <p>£250</p>
                  </div>
                  <div class="buy">
                    <ion-icon name="cart" style="font-size: 1.5em"></ion-icon>
                  </div>
                </div>
                <div class="right">
                  <div class="done">
                    <ion-icon name="checkmark-done-circle"></ion-icon>
                  </div>
                  <div class="details">
                    <h3>Chair</h3>
                    <p>Added to your cart</p>
                  </div>
                  <div class="remove">
                    <ion-icon name="close-circle"></ion-icon>
                  </div>
                </div>
              </div>
              {{else}}
              <div class="bottom">
                <div class="left">
                  <div class="details">
                    {{#if productPermalink}}
                    <a href="/products/{{this.productPermalink}}">
                      {{this.productTitle}}
                    </a>
                    {{else}}
                    <a href="/products/{{this._id}}">
                      {{this.productTitle}}
                    </a>
                    {{/if}}
                    <p>
                      {{currencySymbol @root.config.currencySymbol}}{{formatAmount productPrice}}
                    </p>
                  </div>
                  <div class="buy">
                    {{#if @root.config.showHomepageVariants}}
                    <a class="add-variant-to-cart" data-id="{{this._id}}" role="button">
                      <ion-icon name="cart"></ion-icon>
                    </a>
                    {{else}}
                    <a class="add-to-cart" data-id="{{this._id}}" data-link="{{this.productPermalink}}"
                      data-has-variants="{{checkProductVariants this.variants}}" role="button">
                      <ion-icon name="cart"></ion-icon>
                    </a>
                    {{/if}}
                  </div>
                </div>
              </div>
              {{/if}}
            </div>
            <div class="inside">
              <div class="icon">
                <ion-icon name="information-circle"></ion-icon>
              </div>
              <div class="contents">
                <h4>
                  {{#if productPermalink}}
                  <a href="/products/{{this.productPermalink}}">
                    {{this.productTitle}}
                  </a>
                  {{else}}
                  <a href="/products/{{this._id}}">
                    {{this.productTitle}}
                  </a>
                  {{/if}}
                </h4>
                <h3>
                  {{currencySymbol @root.config.currencySymbol}}{{formatAmount productPrice}}
                </h3>
              </div>
            </div>
          </div>
        </div>
        {{/each}}

      </div>
      <!-- /.row -->

    </div>
    <!-- /.col-lg-9 -->

  </div>
</div>
{{> themes/MarieLyne/footer}}

{{!-- {{#each results}}
  <div class="col-12 col-md-4 col-sm-6 col-xs-12 product-men">
    <div class="product-shoe-info shoe">
      <div class="men-pro-item">
        {{#if productPermalink}}
        <div class="men-thumb-item shop-page-product">
          {{#if productImage}}
          <img src="{{this.productImage}}" alt="">
          {{else}}
          <img src="/uploads/placeholder.png" alt="">
          {{/if}}
          <div class="men-cart-pro">
            <div class="inner-men-cart-pro">
              <a href="/product/{{this.productPermalink}}" class="link-product-add-cart">Quick View</a>
            </div>
          </div>
          {{!-- <span class="product-new-top">New</span> --}}
</div>
{{else}}
<div class="men-thumb-item shop-page-product">
  {{#if productImage}}
  <img src="{{this.productImage}}" alt="">
  {{else}}
  <img src="/uploads/placeholder.png" alt="">
  {{/if}}
  <div class="men-cart-pro">
    <div class="inner-men-cart-pro">
      <a href="/product/{{this._id}}" class="link-product-add-cart">Quick View</a>
    </div>
  </div>
  {{!-- <span class="product-new-top">New</span> --}}
</div>
{{/if}}
<div class="item-info-product">
  <h4>
    {{#if productPermalink}}
    <a href="/products/{{this.productPermalink}}">{{this.productTitle}} </a>
    {{else}}
    <a href="/products/{{this._id}}">{{this.productTitle}} </a>
    {{/if}}
  </h4>
  {{#if variants}}
  <div class="info-product-price">
    {{#if @root.config.showHomepageVariants}}
    <select id="productVariant-{{this._id}}" class="productVariant form-control mb-2">
      {{#each variants}}
      <option value="{{this._id}}" data-price="{{this.price}}">
        {{this.title}} - {{currencySymbol @root.config.currencySymbol}}{{this.price}}
      </option>
      {{/each}}
    </select>
    {{else}}
    <h4 class="product-price mp-0 text-center">
      {{currencySymbol @root.config.currencySymbol}}{{formatAmount variants.0.price}}
    </h4>
    {{/if}}
    <div class="buy">
      {{#if @root.config.showHomepageVariants}}
      <a class="add-variant-to-cart mainBtn cartBtn" data-id="{{this._id}}" role="button">
        Add to Cart <i class="fa fa-cart-plus" aria-hidden="true"></i>
      </a>
      {{else}}
      <a class="add-to-cart mainBtn cartBtn" data-id="{{this._id}}" data-link="{{this.productPermalink}}"
        data-has-variants="{{checkProductVariants this.variants}}" role="button"> Add to Cart
        <i class="fa fa-cart-plus" aria-hidden="true"></i>
      </a>
      {{/if}}
    </div>
  </div>
  {{else}}
  <div class="info-product-price">
    <div class="grid_meta">
      <div class="product_price">
        <div class="grid-price ">
          <span class="money">
            {{currencySymbol @root.config.currencySymbol}}{{formatAmount productPrice}}
          </span>
        </div>
      </div>
    </div>
    <div class="buy">
      {{#if @root.config.showHomepageVariants}}
      <a class="add-variant-to-cart mainBtn cartBtn" data-id="{{this._id}}" role="button"> Add to
        cart
        <i class="fa fa-cart-plus" aria-hidden="true"></i>
      </a>
      {{else}}
      <a class="add-to-cart  mainBtn cartBtn" data-id="{{this._id}}" data-link="{{this.productPermalink}}"
        data-has-variants="{{checkProductVariants this.variants}}" role="button">Add To cart
        <i class="fa fa-cart-plus" aria-hidden="true"></i>
      </a>
      {{/if}}
    </div>
  </div>
  {{/if}}
  <div class="clearfix"></div>
</div>
</div>
</div>
</div>
{{/each}} --}}